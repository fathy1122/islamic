import 'package:flutter/material.dart';

class Radi extends StatelessWidget {
  const Radi({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
    );
  }
}
